pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
    repositories {
        jcenter()
        maven { url 'https://jitpack.io' }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
    repositories {
        jcenter()
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "Brandat"
include ':app'
